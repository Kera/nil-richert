name: Deploy dev
env:
  CURRENT_ENV: develop
  AWS_LOGIN: ${{ secrets.AWS_ACCOUNT_EMAIL }}
on:
  workflow_dispatch:
  push:
    branches:
      - develop
jobs:
  deploy:
    uses: ./.github/workflow/deploy.yml